using System;
using Xunit;
using ContactManager.Services;
using ContactManager.Models;
using ContactManager.ViewModels;
using ContactManager.Infrastructure;

namespace ContactManager.UnitTest
{
    public class UnitTest1
    {
        //private readonly ContactManagerTestContext dbContext;

        //public UnitTest1(ContactManagerTestContext dbContext)
        //{
        //    this.dbContext = dbContext;
        //}
        //[Fact]
        //public void Test1()
        //{
        //    var list = new ContactList();
        //    foreach (var contact in dbContext.Contacts)
        //    {
        //        list.Contacts.Add(new ContactList.Contact
        //        {
        //            Id = contact.Id,
        //            Name = contact.Name,
        //            Company = contact.Company,
        //            Email = contact.Email,
        //            PhoneNumber = contact.PhoneNumber
        //        });
        //    }
        //    var expected = list.Contacts.Count;
        //    var actual = 1;
        //    Assert.Equal(expected, actual);
        //}
        
        [Fact]
        public void test1()
        {
            
        }
    }
}
